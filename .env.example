# Credentials for the local MySQL container (docker-compose.yml).
# Copy this file to .env and replace the placeholder values.
# The database names (food_search, food_search_test) and the app user are fixed.
MYSQL_ROOT_PASSWORD=replace-me
MYSQL_PASSWORD=replace-me
MYSQL_PORT=3306
